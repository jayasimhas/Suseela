@inherits GlassView<RelatedCompaniesModel>

@if (Model.RelatedCompanies != null && Model.RelatedCompanies.Any())
{
    <div class="article-topics">
        <h2>@Model.ComponentTitle</h2>
        <ul>
            @foreach (var company in Model.RelatedCompanies)
            {
                <li class="article-topics__li">@RenderLink(company, m => m)</li>
            }
        </ul>
    </div>
}
