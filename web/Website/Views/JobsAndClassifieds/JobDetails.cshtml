@model Informa.Web.ViewModels.JobsAndClassifieds.JobDetailsViewModel

<div class="main__content">
    <div>
        <div class="action-flags-bar article-prologue"></div>
        <div class="job_detail">
            <h1>@Model.JobTitle</h1>
            <div class="sub-heading-text">
                <p>@Model.JobSubTitle</p>
                <p>@Model.PublishedText @Model.PublishedDate.ToString("d MMMM yyyy") @Model.PublishedDate.ToShortTimeString()</p>
            </div>
            <div class="job_logo">
                <a href="#">
                    <img src="@Model.JobLogo.Src" alt="logo" align="center">
                </a>
            </div>
            <div class="job_description">
                <p>@Html.Raw(Model.JobDescription)</p>
                
            </div>
        </div>


    </div>
</div>